language: julia

os:
  - linux

julia:
  - nightly

matrix:
  allow_failures:
    - julia: nightly

before_install:
 - julia install.jl

## uncomment the following lines to override the default test script
script:
 - julia test/CircuitParserTest.jl
 - julia test/CircuitQueriesTest.jl


notifications:
  slack: 
    on_success: always
    on_failure: always
    rooms: 
      - $SLACK_NOTIF